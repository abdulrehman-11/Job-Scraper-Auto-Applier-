# Render.com configuration for Job Scraper API
# https://render.com/docs/yaml-spec

services:
  - type: web
    name: job-scraper-api
    env: python
    region: oregon  # or your preferred region
    plan: free  # or starter, standard, pro
    buildCommand: "pip install -r requirement.txt && playwright install --with-deps chromium"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 600 --keep-alive 5 --log-level info api:app"
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: False
      - key: HEADLESS_MODE
        value: True
      - key: MAX_PAGES_PER_KEYWORD
        value: 3
      - key: PORT
        generateValue: true
