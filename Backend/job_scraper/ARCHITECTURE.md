# 🏗️ Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         n8n Workflow                         │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │  Schedule   │→ │ HTTP Request │→ │  Process Data    │  │
│  │  Trigger    │  │   (POST)     │  │ Filter & Store   │  │
│  └─────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          │ HTTP POST /api/scrape-jobs
                          │ { "platform": "...", "keywords": [...] }
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                    Job Scraper API (Flask)                   │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  api.py                                                 │ │
│  │  ┌─────────────────────────────────────────────────┐  │ │
│  │  │  POST /api/scrape-jobs                          │  │ │
│  │  │  ├─ Validate parameters                         │  │ │
│  │  │  ├─ Start heartbeat thread (5 min intervals)    │  │ │
│  │  │  ├─ Call JobScraper                             │  │ │
│  │  │  ├─ Format response for n8n                     │  │ │
│  │  │  └─ Return JSON with jobs                       │  │ │
│  │  └─────────────────────────────────────────────────┘  │ │
│  │                                                         │ │
│  │  GET /health  │  GET /api/status  │  GET /           │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          │ asyncio.run(run_scraper(...))
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                  Job Scraper (Playwright)                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Screp.py - JobScraper class                           │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │ │
│  │  │ SimplyHired  │  │  Talent.com  │  │  Glassdoor  │ │ │
│  │  │   Scraper    │  │   Scraper    │  │   Scraper   │ │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬──────┘ │ │
│  │         │                  │                  │         │ │
│  │         └──────────────────┴──────────────────┘         │ │
│  │                            │                             │ │
│  │  ┌─────────────────────────┴───────────────────────┐   │ │
│  │  │  Common Processing                              │   │ │
│  │  │  • Extract full descriptions                    │   │ │
│  │  │  • Parse dates to ISO format                    │   │ │
│  │  │  • Generate unique job IDs                      │   │ │
│  │  │  • Remove duplicates                            │   │ │
│  │  │  • Filter last 24 hours                         │   │ │
│  │  └─────────────────────────────────────────────────┘   │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          │ Return List[Dict] of jobs
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      Response (JSON)                         │
│  {                                                           │
│    "success": true,                                          │
│    "total_jobs": 20,                                         │
│    "scraped_at": "2025-11-04T22:30:00",                     │
│    "jobs": [...]                                             │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## Request Flow Diagram

```
Client (n8n)
    │
    │ 1. POST /api/scrape-jobs
    │    Body: {"platform": "SimplyHired", "keywords": ["python"], "pages": 1}
    ↓
┌─────────────────────┐
│  Flask API (api.py) │
└─────────────────────┘
    │
    │ 2. Validate parameters
    │    ✓ keywords is list
    │    ✓ pages is positive integer
    ↓
    │ 3. Start heartbeat thread
    │    (logs every 5 min)
    ↓
    │ 4. Call asyncio.run(run_scraper(...))
    ↓
┌─────────────────────────┐
│  Job Scraper (Screp.py) │
└─────────────────────────┘
    │
    │ 5. Initialize JobScraper(headless=True)
    ↓
    │ 6. Launch Playwright browser
    ↓
    │ 7. For each keyword:
    │    ├─ Navigate to search page
    │    ├─ For each page:
    │    │  ├─ Extract job cards
    │    │  ├─ Click job to load details
    │    │  ├─ Extract full description
    │    │  ├─ Parse posted date
    │    │  └─ Store job data
    │    └─ Next page
    ↓
    │ 8. Remove duplicates
    │    (based on company + title)
    ↓
    │ 9. Filter last 24 hours
    │    (based on posted_date)
    ↓
    │ 10. Return jobs list
    ↓
┌─────────────────────┐
│  Flask API (api.py) │
└─────────────────────┘
    │
    │ 11. Format for n8n
    │     • Add "success": true
    │     • Add "total_jobs": count
    │     • Rename "source" to "source_api"
    ↓
    │ 12. Return JSON response (200 OK)
    ↓
Client (n8n)
    │
    │ 13. Receive jobs data
    │ 14. Process in workflow
    │     ├─ Split in batches
    │     ├─ Filter (full-time)
    │     ├─ Send to Slack
    │     └─ Save to Sheets
```

---

## Error Handling Flow

```
Client sends request
    │
    ↓
API receives request
    │
    ├─ Invalid parameters? ──→ 400 Bad Request
    │                          {"success": false, "error": "..."}
    │
    ├─ Scraping starts
    │   │
    │   ├─ Platform error? ──→ Log error, continue other platforms
    │   │
    │   ├─ All platforms fail? ──→ 500 Internal Server Error
    │   │                          {"success": false, "error": "..."}
    │   │
    │   └─ Success ──→ 200 OK
    │                  {"success": true, "jobs": [...]}
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        GitHub Repository                     │
│  • api.py, Screp.py, config.py                              │
│  • render.yaml, Procfile, requirement.txt                   │
│  • Documentation files                                       │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ git push
                      ↓
┌─────────────────────────────────────────────────────────────┐
│                          Render.com                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Build Process                                       │   │
│  │  1. Detect render.yaml                              │   │
│  │  2. Install Python 3.11                             │   │
│  │  3. pip install -r requirement.txt                  │   │
│  │  4. playwright install --with-deps chromium         │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Runtime                                             │   │
│  │  • Gunicorn starts with:                            │   │
│  │    - 1 worker                                       │   │
│  │    - 2 threads                                      │   │
│  │    - 600s timeout                                   │   │
│  │  • Health checks every 60s                          │   │
│  │  • Auto-restart on failure                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                          ↓                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Public URL                                          │   │
│  │  https://your-app.onrender.com                      │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          │ HTTPS requests
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                        n8n / Clients                         │
│  Can call API from anywhere with:                           │
│  POST https://your-app.onrender.com/api/scrape-jobs         │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow

```
Input (API Request)
    │
    │ {"platform": "SimplyHired", "keywords": ["python"], "pages": 2}
    ↓
┌──────────────────────┐
│  Parameter Parsing   │
│  • platform → "SimplyHired"
│  • keywords → ["python"]
│  • pages → 2 (max 5)
│  • location → "United States" (default)
└──────────────────────┘
    │
    ↓
┌──────────────────────┐
│  Browser Launch      │
│  • Headless Chrome
│  • Random user agent
│  • Anti-detection
└──────────────────────┘
    │
    ↓
┌──────────────────────┐
│  Page 1 Scraping     │
│  • Load search page
│  • Extract 20 jobs
│  • Click each job
│  • Get full description
└──────────────────────┘
    │
    ↓
┌──────────────────────┐
│  Page 2 Scraping     │
│  • Click next
│  • Extract 20 jobs
│  • Get descriptions
└──────────────────────┘
    │
    ↓
┌──────────────────────┐
│  Raw Jobs Data       │
│  • 40 jobs total
│  • Some duplicates
│  • Mixed dates
└──────────────────────┘
    │
    ↓
┌──────────────────────┐
│  Deduplication       │
│  • Remove exact duplicates
│  • Keep unique jobs
│  • 35 unique jobs
└──────────────────────┘
    │
    ↓
┌──────────────────────┐
│  Date Filtering      │
│  • Keep last 24h only
│  • Remove old jobs
│  • 20 recent jobs
└──────────────────────┘
    │
    ↓
┌──────────────────────┐
│  Format for n8n      │
│  • Add success: true
│  • Add total_jobs: 20
│  • Add scraped_at
│  • Rename fields
└──────────────────────┘
    │
    ↓
Output (API Response)
    │
    │ {
    │   "success": true,
    │   "total_jobs": 20,
    │   "scraped_at": "2025-11-04T22:30:00",
    │   "jobs": [20 job objects]
    │ }
```

---

## Component Interactions

```
┌─────────────┐
│   api.py    │  Main Flask application
└──────┬──────┘
       │
       │ imports
       ↓
┌─────────────┐
│  Screp.py   │  Job scraper engine
└──────┬──────┘
       │
       │ uses
       ↓
┌─────────────┐
│  config.py  │  Configuration values
└─────────────┘

       ┌─────────────┐
       │ Flask       │  Web framework
       └─────────────┘
              │
              │ uses
              ↓
       ┌─────────────┐
       │ Playwright  │  Browser automation
       └─────────────┘
              │
              │ controls
              ↓
       ┌─────────────┐
       │ Chromium    │  Headless browser
       └─────────────┘
```

---

## Heartbeat Mechanism

```
Main Thread                 Heartbeat Thread
    │                            │
    │ Start scraping             │
    │ ──────────────────────────→│ Start timer
    │                            │
    │ Scraping...                │ Wait 5 min
    │                            │
    │ Still scraping...          │ Timer expires
    │                            ├─→ Log "Still scraping... X jobs"
    │                            │
    │ Still scraping...          │ Wait 5 min
    │                            │
    │ Complete! (20 jobs)        │ Timer expires
    ├─→ Stop heartbeat           ├─→ Log "Still scraping... 20 jobs"
    │                            │
    │ Return response            │ Thread exits
    ↓                            ↓
```

---

## File Structure

```
job_scraper/
│
├─ Core Application
│  ├─ api.py              ← Flask REST API (main entry)
│  ├─ Screp.py            ← Job scraper engine
│  └─ config.py           ← Configuration
│
├─ Configuration
│  ├─ requirement.txt     ← Python dependencies
│  ├─ render.yaml         ← Render deployment
│  ├─ Procfile            ← Process definition
│  ├─ runtime.txt         ← Python version
│  ├─ .env.example        ← Environment template
│  └─ .gitignore          ← Git ignore rules
│
├─ Documentation
│  ├─ README.md           ← Main documentation
│  ├─ README_API.md       ← API reference
│  ├─ QUICKSTART.md       ← Quick start guide
│  ├─ DEPLOYMENT.md       ← Deployment guide
│  ├─ PACKAGE_SUMMARY.md  ← Package overview
│  ├─ CHECKLIST.md        ← Implementation checklist
│  └─ ARCHITECTURE.md     ← This file
│
├─ Testing
│  ├─ test_api.py         ← Automated tests
│  └─ verify_setup.py     ← Setup verification
│
├─ Examples
│  └─ n8n_workflow_example.json  ← n8n workflow
│
└─ Setup Scripts
   ├─ setup.ps1           ← Windows installation
   └─ setup.sh            ← Linux/Mac installation
```

---

## Technology Stack

```
┌─────────────────────────────────────────┐
│  Application Layer                      │
│  • Python 3.11                          │
│  • Flask 3.0 (Web framework)            │
│  • Flask-CORS (Cross-origin)            │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────┴───────────────────────┐
│  Scraping Layer                         │
│  • Playwright 1.40 (Browser automation) │
│  • Chromium (Headless browser)          │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────┴───────────────────────┐
│  Server Layer                           │
│  • Gunicorn 21.2 (WSGI server)          │
│  • Render.com (Hosting)                 │
└─────────────────────────────────────────┘
```

---

## Scaling Considerations

```
Current Setup (Single Worker)
┌────────────────────────────┐
│  Gunicorn                  │
│  ├─ Worker 1               │
│  │  ├─ Thread 1 (handles requests)
│  │  └─ Thread 2 (handles requests)
└────────────────────────────┘

Limitations:
• Max 2 concurrent requests
• Playwright requires single worker
• Long-running requests block others

Future Improvements:
• Use Redis queue for async processing
• Multiple workers with task queue
• Webhook notifications
• Result caching
```

---

This architecture document provides visual representations of:
1. System architecture
2. Request flow
3. Error handling
4. Deployment process
5. Data transformation
6. Component interactions
7. File structure
8. Technology stack

All diagrams use ASCII art for universal compatibility.
